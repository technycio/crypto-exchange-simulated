{
  "name": "blockchain_nodejs_example_quick",
  "version": "1.0.0",
  "description": "A blockchain simulation using TypeScript with MVC architecture",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test:basic": "npm run build && node dist/test-basic.js",
    "test:full": "npm run build && node dist/test-comprehensive.js",
    "test:workflow": "npm run build && node dist/test-full-workflow.js",
    "test:simulation": "npm run build && node dist/test-simulation.js",
    "cli": "npm run build && node dist/cli-main.js"
  },
  "keywords": [
    "blockchain",
    "crypto",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/inquirer": "^9.0.9",
    "@types/node": "^24.8.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^11.1.0",
    "inquirer": "^9.3.8"
  }
}
